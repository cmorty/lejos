package lejos.robotics.probability;

import lejos.robotics.Movement;

/**
 * TODO: This needs to extend Movement somehow, but can't extend an enumerator because already implicitly extends Enum
 *
 */
public class QualifiedMovement {
	
	public float getError() {
		return 0;
	}
}
